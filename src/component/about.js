import React from 'react'
import { Title, AboutDescription } from '../styles'

function About() {
    return <>
        <Title>Github Search</Title>
        <AboutDescription>This is a small exercise that uses the GitHub API, allows to perform users and repositories search.</AboutDescription>
    </>;
}

export default About;